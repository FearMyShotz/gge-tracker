name: copilot-setup-steps

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on:
      - self-hosted
      - Linux
      - X64
    steps:
      - name: Runner ready
        run: echo "Self-hosted runner is available"
